/** 前端／後端共用預設文案（勿放 Node fs） */
export const DEFAULT_MARQUEE =
  "歡迎使用訪客服務機｜請選擇訪客報到、簽退或現場預約";

/** 無操作倒數回首頁秒數 */
export const KIOSK_IDLE_SECONDS = 20;

/** 出口開閘時限預設（分鐘） */
export const EXIT_GATE_MINUTES_DEFAULT = 15;

export type RecordsFilter = "all" | "on_site" | "temp_out" | "departed";
